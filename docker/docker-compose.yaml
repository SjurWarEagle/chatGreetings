version: '3.3'

services:
  twitch-greetings:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    environment:
      - CONFIG-FILE:/project/private_config.json
    volumes:
      - ../../src/main/resources/private_config.json:/project/private_config.json:ro
#    ports:
#      - 96:96
    restart: unless-stopped
